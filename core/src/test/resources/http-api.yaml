openapi: 3.0.3
info:
  title: HTTP API
  version: "1.0"
servers:
  - url: http://api.example.com
paths:
  /pets/{id}:
    get:
      summary: Get a pet
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
