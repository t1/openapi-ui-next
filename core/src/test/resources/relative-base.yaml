openapi: "3.1.0"
info:
  title: Relative Base API
  version: "1.0"
servers:
  - url: /
paths:
  /items:
    get:
      operationId: listItems
      summary: List items
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
