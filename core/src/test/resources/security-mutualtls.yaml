openapi: 3.0.3
info:
  title: Security Test API - Mutual TLS
  version: 1.0.0
servers:
  - url: https://api.example.com
components:
  securitySchemes:
    MutualTLS:
      type: mutualTLS
      description: Mutual TLS authentication
security:
  - MutualTLS: []
paths:
  /pets:
    get:
      summary: List pets
      operationId: listPets
      responses:
        '200':
          description: A list of pets
