openapi: 3.0.3
info:
  title: HTTP Localhost API
  version: "1.0"
servers:
  - url: http://localhost:8080
paths:
  /pets/{id}:
    get:
      summary: Get a pet
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
